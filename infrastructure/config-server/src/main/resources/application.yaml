spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/launch-gate/launch-gate-config-repo.git
          default-label: main
server:
  port: 8888
