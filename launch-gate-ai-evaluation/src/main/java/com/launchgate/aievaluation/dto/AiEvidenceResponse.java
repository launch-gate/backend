package com.launchgate.aievaluation.dto;

/**
 * Ответ со подтверждием, на основе которого ИИ сформировал оценку.
 * Используется для подтверждения выводов нейросети конкретными фактами из работы участника.
 *
 * @param path       путь к файлу или источнику данных, где найден фрагмент
 * @param chunkIndex индекс текстового блока (чанка), в котором была обнаружена информация
 * @param quote      точная цитата или фрагмент кода из решения участника
 * @param why        обоснование ИИ, почему этот фрагмент является доказательством
 */
public record AiEvidenceResponse(
        String path,
        Integer chunkIndex,
        String quote,
        String why
) {
}
